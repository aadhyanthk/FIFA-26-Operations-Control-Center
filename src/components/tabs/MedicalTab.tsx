import React from 'react';

export const MedicalTab: React.FC = () => {
  return (
    <div style={{ padding: 'var(--space-md)' }}>
      <h2 style={{ color: 'var(--text-primary)' }}>Medical Operations</h2>
      <p style={{ color: 'var(--text-secondary)' }}>Active incidents and response times...</p>
    </div>
  );
};
