import React from 'react';

export const SecurityTab: React.FC = () => {
  return (
    <div style={{ padding: 'var(--space-md)' }}>
      <h2 style={{ color: 'var(--text-primary)' }}>Security Operations</h2>
      <p style={{ color: 'var(--text-secondary)' }}>Security team deployments and scanner status...</p>
    </div>
  );
};
